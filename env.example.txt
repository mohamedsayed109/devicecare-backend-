# Copy to .env.local for local development (or set these in your host's
# environment variables panel for production).

# Postgres connection string. Render/Railway/Neon all give you this directly
# after creating a database.
DATABASE_URL=postgres://user:password@host:5432/devicecare

# Set to "false" only for local Postgres without SSL. Leave "true" (or unset)
# for any hosted database.
DATABASE_SSL=true

# Shared secret for the admin/dashboard endpoints. Generate a long random
# value - this is a placeholder until real per-user login is built.
ADMIN_KEY=change-me-to-a-long-random-string

PORT=3000
